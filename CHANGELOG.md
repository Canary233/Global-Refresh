# 更新日志

## 1.4.1

- 修复 WebUI 全局高刷开关初始化后保持禁用、无法点击的问题。

## 1.4.0

- 新增全局高刷开关；开启后按目标档位锁定整个系统的刷新率。
- 关闭全局高刷后，可为每个已选应用单独设置自动或任意系统支持的刷新率档位。
- 兼容旧版 `high_refresh_apps` 配置；首次保存应用设置时会自动迁移为单应用档位。

## 1.3.6

- 模块升级时保留已有 `refresh.conf`，包括已选择的高刷应用。
- 默认配置改为仅在首次安装或配置缺失时生成，避免 OTA 覆盖用户设置。

## 1.3.5

- WebUI 先读取刷新率状态，应用列表在后台独立加载，避免阻塞页面。
- 缓存系统应用名称；仅在安装或卸载应用后重新解析名称。

## 1.3.4

- WebUI 显示系统应用名称，并保留包名作为辅助信息。
- 已勾选的高刷应用固定显示在列表顶部；勾选或取消后立即重排。

## 1.3.3

- 新增 WebUI 高刷应用筛选和搜索。
- 仅在已选应用处于前台时锁定刷新率；切换离开后恢复原有刷新率设置。
- 在 HyperOS 上为已选应用使用场景刷新率策略，修复视频播放时可能回落至 60Hz 的问题。
- 新增 KernelSU OTA 更新元数据。
